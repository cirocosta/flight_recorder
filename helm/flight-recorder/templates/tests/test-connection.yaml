apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flight-recorder.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "flight-recorder.name" . }}
    helm.sh/chart: {{ include "flight-recorder.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flight-recorder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
