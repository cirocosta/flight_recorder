apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flight_recorder.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "flight_recorder.name" . }}
    helm.sh/chart: {{ include "flight_recorder.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flight_recorder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
